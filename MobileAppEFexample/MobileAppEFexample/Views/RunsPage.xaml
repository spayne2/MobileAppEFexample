<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="MobileAppEFexample.RunsPage"
    Title="Runs">
    <ContentPage.Content>
        <CollectionView x:Name="runsCollection">

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Padding="10">
                        <Grid>
                            <Grid.RowDefinitions>
                               <RowDefinition Height="15" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Label Grid.Column="0" Text="Run:"/>
                            <Label Grid.Column="1" Text="{Binding DistanceInKM}" />
                            <Label Grid.Column="2" Text="{Binding TimeInMins}" />
                            <Label Grid.Column="3" Text="{Binding TimeInSeconds}" />
                            <Label Grid.Column="4" Text="{Binding Terrain}" />
                        </Grid>                        
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>

            <CollectionView.EmptyView>
                <StackLayout>
                    <Label VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand"
                           Text="No posts for this blog"/>
                </StackLayout>
            </CollectionView.EmptyView>

        </CollectionView>
    </ContentPage.Content>

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" Clicked="ToolbarItem_Clicked"/>
    </ContentPage.ToolbarItems>
</ContentPage>